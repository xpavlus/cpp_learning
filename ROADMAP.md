# ROADMAP — план тем

| # | Тема | Статус |
|---|------|--------|
| 01 | **Setup** — установка окружения, CMake, первая сборка | 🔄 В процессе |
| 02 | **Basics** — Hello World, компиляция, `main`, `iostream`, `std::cout` | 🔄 В процессе |
| 03 | **Control Flow** — `if/else`, `switch`, `for`, `while`, `do-while` | ⬜ Планируется |
| 04 | **Functions** — объявление, перегрузка, значения по умолчанию, inline | ⬜ Планируется |
| 05 | **OOP** — классы, конструкторы, деструкторы, наследование, полиморфизм | ⬜ Планируется |
| 06 | **Templates** — функции-шаблоны, шаблоны классов, специализация | ⬜ Планируется |
| 07 | **STL** — `vector`, `string`, `map`, `set`, `array`, итераторы | ⬜ Планируется |
| 08 | **Algorithms** — `<algorithm>`, `<numeric>`, диапазоны (C++20 ranges) | ⬜ Планируется |
| 09 | **Memory** — стек/куча, `new/delete`, умные указатели (`unique_ptr`, `shared_ptr`) | ⬜ Планируется |

---

> Каждая тема живёт в своей папке `lessons/NN_<name>/`.  
> Заметки — в `notes.md`, задания — в `src/`.
